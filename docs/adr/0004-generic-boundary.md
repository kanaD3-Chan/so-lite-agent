# 通用运行时边界：事件/审计/中断只收通用子集，业务语义留使用方

事件流、审计记录与中断信号只包含通用运行时语义：消息增量、工具起止/进度、回合结束、会话切换、压缩、错误，以及 `ConfigChanged`（mistake-agent 的 `SettingsChanged` 通用化改名）。记忆变更、验算请求、余额/缓存统计、Memory/Compute 审计等业务记录一律不进 crate，由使用方在自己的协议层扩展。理由：crate 没有对应的内置服务，把业务语义焊进泛化接口会让公共面变宽且无法自洽；通用化改名同时落地了原计划 M1 清单中的解耦项。
